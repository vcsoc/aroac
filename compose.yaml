services:
  oar:
    build: .
    ports:
      - "127.0.0.1:3001:3001"
    environment:
      COOKIE_SECURE: ${COOKIE_SECURE:-false}
      APP_ORIGIN: ${APP_ORIGIN:-http://localhost:3001}
    volumes:
      - oar-data:/app/data
    restart: unless-stopped
volumes:
  oar-data:
